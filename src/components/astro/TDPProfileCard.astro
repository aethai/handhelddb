---
interface TDPProfile {
  tdpWatts: number;
  fpsAvg: number;
  resolution: string;
  preset: string;
  fsrEnabled: boolean;
  fsrMode?: string;
  estimatedBatteryHours: number;
  thermal: string;
  fanNoise: string;
  reportCount: number;
  confidence: string;
}

interface Props {
  profile: TDPProfile;
  tier: 'battery_saver' | 'balanced' | 'performance';
}

const { profile, tier } = Astro.props;

const tierConfig = {
  battery_saver: {
    label: 'Battery Saver',
    icon: '🔋',
    color: 'copper',
    border: 'border-[#7c6cf0]/30',
    bg: 'bg-[#7c6cf0]/5',
    accent: 'text-[#7c6cf0]',
  },
  balanced: {
    label: 'Balanced',
    icon: '⚖️',
    color: 'copper',
    border: 'border-[#7c6cf0]/30',
    bg: 'bg-[#7c6cf0]/5',
    accent: 'text-[#7c6cf0]',
  },
  performance: {
    label: 'Performance',
    icon: '🚀',
    color: 'orange',
    border: 'border-orange-500/30',
    bg: 'bg-orange-500/5',
    accent: 'text-orange-400',
  },
};

const config = tierConfig[tier];

function fpsColor(fps: number): string {
  if (fps >= 60) return 'text-green-400';
  if (fps >= 40) return 'text-yellow-400';
  if (fps >= 30) return 'text-orange-400';
  return 'text-red-400';
}

const thermalLabels: Record<string, string> = { cool: 'Cool', warm: 'Warm', hot: 'Hot' };
const fanLabels: Record<string, string> = { silent: 'Silent', quiet: 'Quiet', audible: 'Audible', loud: 'Loud' };
---

<div class={`rounded-xl border ${config.border} ${config.bg} p-5`}>
  <div class="flex items-center justify-between mb-4">
    <div class="flex items-center gap-2">
      <span class="text-lg">{config.icon}</span>
      <h4 class={`text-sm font-semibold ${config.accent}`}>{config.label}</h4>
    </div>
    <span class="rounded-full bg-[#1a1a22] px-2 py-0.5 text-[10px] text-[#8a8a94]">
      {profile.reportCount} report{profile.reportCount !== 1 ? 's' : ''}
    </span>
  </div>

  <!-- Big FPS number -->
  <div class="text-center mb-4">
    <p class={`text-4xl font-bold font-mono ${fpsColor(profile.fpsAvg)}`}>{Math.round(profile.fpsAvg)}</p>
    <p class="text-xs text-[#55555e] mt-1">avg FPS</p>
  </div>

  <!-- Settings grid -->
  <div class="space-y-2 text-sm">
    <div class="flex justify-between">
      <span class="text-[#55555e]">TDP</span>
      <span class="text-white font-medium font-mono">{profile.tdpWatts}W</span>
    </div>
    <div class="flex justify-between">
      <span class="text-[#55555e]">Resolution</span>
      <span class="text-[#8a8a94]">{profile.resolution}</span>
    </div>
    <div class="flex justify-between">
      <span class="text-[#55555e]">Preset</span>
      <span class="text-[#8a8a94] capitalize">{profile.preset.replace('_', ' ')}</span>
    </div>
    {profile.fsrEnabled && (
      <div class="flex justify-between">
        <span class="text-[#55555e]">FSR/DLSS</span>
        <span class="text-[#8a8a94] capitalize">{profile.fsrMode ?? 'On'}</span>
      </div>
    )}
    <div class="border-t border-[#1a1a22] my-2"></div>
    <div class="flex justify-between">
      <span class="text-[#55555e]">Battery</span>
      <span class="text-[#8a8a94] font-mono">{profile.estimatedBatteryHours.toFixed(1)}h</span>
    </div>
    <div class="flex justify-between">
      <span class="text-[#55555e]">Thermal</span>
      <span class="text-[#8a8a94]">{thermalLabels[profile.thermal] ?? profile.thermal}</span>
    </div>
    <div class="flex justify-between">
      <span class="text-[#55555e]">Fan</span>
      <span class="text-[#8a8a94]">{fanLabels[profile.fanNoise] ?? profile.fanNoise}</span>
    </div>
  </div>
</div>
